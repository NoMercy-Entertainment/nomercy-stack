[Definition]
# Aggressive detection of directory scanning and reconnaissance attempts
# This filter catches systematic scanning patterns that indicate malicious intent

# Match directory scanning patterns that return 404
failregex = ^.*".*(?:old|new|backup|test|wordpress|wp-|admin|login|manager|phpmyadmin|webmail|mail|ftp|sql|database|db|api|app|config|setup|install).*".*404.*<HOST>.*$
            ^.*".*(?:20[0-9][0-9]|19[0-9][0-9]).*".*404.*<HOST>.*$

ignoreregex =
