# Public IP this node's authoritative DNS binds to (ns1 = DO, ns2 = Proxmox)
DNS_BIND_IP=203.0.113.10

# Shared bearer token Laravel uses to push/remove ACME TXT records on this node
TXT_UPDATER_TOKEN=change-me-to-a-long-random-secret
